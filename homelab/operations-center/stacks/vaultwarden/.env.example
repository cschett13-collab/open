# Copy to .env and edit. .env is gitignored.

# Generate with:  openssl rand -base64 48
ADMIN_TOKEN=REPLACE_ME

# Leave true to register the first account, then set false and re-up to lock it.
SIGNUPS_ALLOWED=true

# How your clients reach Vaultwarden on the tailnet.
DOMAIN=http://nas:8084
